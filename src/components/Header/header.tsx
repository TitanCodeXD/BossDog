//Components
import Logo from '../Logo/logo';
import Menu from '../Menu/menu';

//Css
import './header.css';

const Header = () => {
    return (
        <header>
            <Logo />
            <Menu />
        </header>
    );
};

export default Header;
