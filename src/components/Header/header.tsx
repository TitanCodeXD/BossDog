//Components
import Button from '../Button/button';
import Logo from '../Logo/logo';

//Css
import './header.css';

const Header = () => {
    return (
        <header>
            <Logo />
            <Button />
        </header>
    );
};

export default Header;
